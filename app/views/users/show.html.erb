<p id="notice"><%= notice %></p>

<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>


<p><%= button_to 'Edit profile', edit_user_path(@user), method: "get" %></p>
<p><%= button_to "All links", urls_path, method: "get" %></p>
<p><%= button_to "Add a new link", new_url_path, method: "get" %></p>

<%= render "search" %>