<%= form_with url: urls_search_path, method: :get do |f| %>
  <h3><%= f.label :inquiry, "Search for:" %></h3>
  <div class="col-sm-10" style="padding: 10px">
    <%= f.text_field :inquiry, class: 'mb-1 form-control' %>
  </div>
  <%= f.submit "Search", class: "mt-2 btn btn-primary" %>
<% end %>